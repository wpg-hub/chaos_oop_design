# UPC 串行进程杀除工作流
# 依次对 upc-nontalker, rc-leader Pod 注入进程杀除故障

workflow:
  id: upc_serial_process_kill_001
  name: UPC 串行进程杀除工作流
  description: 按顺序依次对 upc-nontalker, rc-leader Pod 注入进程杀除故障，测试系统恢复能力
  execution_mode: serial
  auto_clear: false
  
  timing:
    start_delay: 120              # 整体启动延迟 5 秒
    node_interval: 10           # 节点间延迟 10 秒（等待前一个任务完成）
    task_timeout: 300           # 单任务超时 5 分钟
    global_timeout: 1800        # 全局超时 30 分钟

  tasks:
    - id: kill_upc_nontalker
      name: UPC NonTalker 进程杀除
      case:
        name: upc_nontalker_kill
        type: process
        environment: 1_ssh_remote
        fault_type: kill
        pod_match:
          name:
            - upc-nontalker
          random: true
          count: 1
        duration: 240s
        loop_count: 1
        namespace: ns-dupf
        parameters:
          signal: 15
          main_process_pid: 1

    - id: kill_rc_leader
      name: RC Leader 进程杀除
      case:
        name: rc_leader_kill
        type: process
        environment: 1_ssh_remote
        fault_type: kill
        pod_match:
          name:
            - rc-leader
          random: true
          count: 1
        duration: 240s
        loop_count: 1
        namespace: ns-dupf
        parameters:
          signal: 15
          main_process_pid: 1

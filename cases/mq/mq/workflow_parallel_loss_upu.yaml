# MQ 并行网络丢包工作流
# 对 mq Pod 注入丢包故障

workflow:
  id: mq_parallel_network_loss_001
  name: MQ 并行网络丢包工作流
  description: 对 mq Pod 注入丢包故障，测试系统容错能力
  execution_mode: parallel
  auto_clear: true
  
  timing:
    start_delay: 10
    task_timeout: 900
    global_timeout: 1800
    branch_start_delay: 2

  tasks:
    - id: loss_mq
      name: MQ 网络丢包
      case:
        name: mq_loss
        type: network
        environment: 1_ssh_remote
        fault_type: loss
        pod_match:
          name:
            - mq
        duration: 240s
        loop_count: 1
        namespace: ns-dupf
        parameters:
          percent: ["10%", "90%"]
          correlation: ["30%", "90%"]
